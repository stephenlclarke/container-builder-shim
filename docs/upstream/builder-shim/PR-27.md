# Pull request 27: clear inherited builder findings

## Summary

- Refactor build option parsing, image-state resolution, export configuration,
  stream routing, cache insertion, and prefetch reads into focused units.
- Remove dead cached-file branches and unused server state.
- Explain intentionally empty callbacks and preserve exact, documented
  dispositions for required interface contexts, loopback diagnostics, and the
  isolated builder VM's root process.
- Make main and branch checks reject every unresolved issue or hotspot; pull
  requests continue to enforce the previous-version new-code boundary.
- Add direct unit coverage for the extracted export/solve helpers and simplify
  the concurrency stress harness without reducing its workload.

See [issue 26](https://github.com/stephenlclarke/container-builder-shim/issues/26)
and its [repository handoff](ISSUE-26.md).

## Validation

- [x] All Go unit and integration tests
- [x] Full race, goroutine-leak, memory-leak, and stress suite
- [x] Coverage generation
- [x] Go vet and pinned lint suite
- [x] GitHub Actions workflow validation
- [x] Markdown validation
- [ ] Exact-head SonarQube pull-request analysis
- [ ] Exact merged-main SonarQube analysis with zero unresolved issues/hotspots

## Compatibility and risk

The builder protocol and external API are unchanged. The refactor retains the
same platform ordering, Dockerfile metadata semantics, prefetch bounds, retry
behavior, and exporter defaults. Full race and leak evidence covers the changed
concurrency paths.

Closes [#26](https://github.com/stephenlclarke/container-builder-shim/issues/26).

Pull request: [#27](https://github.com/stephenlclarke/container-builder-shim/pull/27)
