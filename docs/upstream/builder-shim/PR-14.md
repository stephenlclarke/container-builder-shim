# Pull request 14: repair matched Compose documentation links

## Summary

- Point the matched Compose status link at `docs/project/STATUS.md`.
- Replace the removed branch guide with the maintained build and worktree guide
  at `docs/guides/BUILD.md`.
- Keep this documentation-only correction outside the immutable builder image;
  the 0.14.0 release continues to select the already-reviewed image digest.

Pull request:
[#14](https://github.com/stephenlclarke/container-builder-shim/pull/14).
Tracking issue:
[#13](https://github.com/stephenlclarke/container-builder-shim/issues/13).

## Evidence

- The GitHub Contents API confirms both exact `main` targets exist.
- Markdownlint is clean for the README and handoff pair.
- `git diff --check` is clean.

## Release impact

No Go source, builder image, tag, or immutable image digest changes. This pull
request only keeps the builder repository's links consistent with the matched
0.14.0 Compose documentation that will be rendered last in the release.
